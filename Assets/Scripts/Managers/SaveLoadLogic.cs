using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SaveLoadLogic : MonoBehaviour
{
    // No singleton. La diferencia del logic con el manager es que este se encarga
    // específicamente de recopilar y restaurar datos, el manager de todo lo demás.
}
