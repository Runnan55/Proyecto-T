using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InputManager : MonoBehaviour
{
    // Singleton. Gestiona los inputs en el juego, tanto teclado y ratón como mando. 

    // Básicamente es como centralizar todos los inputs para hacer una especie de interfaz desde la que poder acceder
    // desde otros scripts sin que sea una guarrada terrible.

    // Al ser prefab singleton tocar las cosas desde el editor.
}
